apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "agea-default.fullname" . }}-test-connection"
  labels:
    {{- include "agea-default.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "agea-default.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
